# =============================================================================
# Grafana Credentials Secret
# =============================================================================
# IMPORTANT: In production, use external secrets management (e.g., Sealed
# Secrets, External Secrets Operator, or HashiCorp Vault) instead of
# committing secrets to version control.
# =============================================================================
---
apiVersion: v1
kind: Secret
metadata:
  name: grafana-credentials
  namespace: monitoring
  labels:
    app: grafana
type: Opaque
stringData:
  admin-user: "admin"
  admin-password: "K8sGrafana@2024!"
